# Copie ce fichier en .crm.env et renseigne les valeurs du CRM distant.
# .crm.env est gitignoré (contient un secret).
# Génère le token côté CRM : php artisan crm:api-token --email=ton@email
CRM_URL=https://crm.exemple.fr
CRM_TOKEN=colle-ton-token-sanctum-ici
